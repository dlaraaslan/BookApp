%div.article-container.mt-5
  %h2.header
    =link_to book.title, book
  - if book.image.present?
    %br
    =image_tag(book.image, class: "article-summary-image")
  %div.body
    =book.body.truncate(500, separator: "...")
    - if book.body.size > 500
      =link_to "Read More", book
  %hr